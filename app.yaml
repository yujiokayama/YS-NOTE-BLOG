# service: ys-note
runtime: nodejs12
handlers:
  - url: /
    static_files: build/index.html
    upload: build/index.html
    secure: always
  - url: /
    static_dir: build
env_variables:
  HOST: "0.0.0.0"
